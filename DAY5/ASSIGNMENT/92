SELECT I.ITEMTYPE,I.DESCR,NVL(TO_CHAR(Q.QUOTATIONID),'	No quotation received') "Available Quotation number" 
  FROM ITEM I 
    LEFT OUTER JOIN QUOTATION Q ON I.ITEMCODE=Q.ITEMCODE 
      WHERE ITEMTYPE IN ('Computer','FMCG')
